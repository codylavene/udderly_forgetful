extends layout.pug
include utils.pug

block content
    +validationErrorSummary(errors)
    form(action='/lists' method='post')
        input(type='hidden' name='_csrf' value=csrfToken)
        +field('Add a List', 'name', null, "add a task...")
        div(class='py-4')
            button(type='submit' class='btn btn-primary') Register
